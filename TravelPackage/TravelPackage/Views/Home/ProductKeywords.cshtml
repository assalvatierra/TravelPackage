@model IEnumerable<TravelPackage.Models.tpKeyword>

@{
    int iCnt = Model.Count();
    int iK = 0;
}

<div class="row">
    @if (iCnt > 0)
    {
        <h1><span style="font-size:medium">Tags</span></h1>
    }

    <div class="col-md-10">
        <p>
            @foreach (var item in Model)
            {
                iK++;
                if (iK > 1)
                {
                    <span>&nbsp;</span>
                }

                @Html.ActionLink(item.Keyword, "Details", new { id = item.Id })

            }
        </p>
    </div>
</div>



